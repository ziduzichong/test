services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./media:/app/media
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
    restart: unless-stopped
